@using Microsoft.AspNetCore.Http
@inject IHttpContextAccessor HttpContextAccessor

@{
    ViewData["Title"] = "Welcome to CRMS";
}

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet" />
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

<style>
    body {
        margin: 0; /* Removes default margin */
        padding: 0; /* Removes default padding */
        font-family: "Poppins", sans-serif;
        background: linear-gradient(135deg, #f5f5f5, #eaeaea);
        min-height: 100vh;
        overflow-x: hidden;
    }

    .hero {
        position: relative;
        height: 100vh;
        background-image: linear-gradient( rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5) ), url('/Images/Homepage/1.jpg');
        background-size: cover;
        background-position: center;
        border-radius: 20px;
        box-shadow: 0 12px 30px rgba(0, 0, 0, 0.1);
        overflow: hidden;
        transition: background-image 1s ease-in-out;
    }

    .hero-content {
        z-index: 2;
    }

        .hero-content h1,
        .hero-content p,
        .hero-content label {
            color: #fff;
        }

    .form-label {
        font-weight: 500;
        font-size: 1rem;
    }

    .btn-warning {
        background-color: #ffc107;
        border: none;
        color: #000;
        transition: background-color 0.3s ease;
    }

        .btn-warning:hover {
            background-color: #e0a800;
            color: #fff;
        }

    .btn-outline-light:hover {
        background-color: #fff;
        color: #000;
    }

    .form-control-lg {
        font-size: 1rem;
    }

    .btn-lg {
        font-size: 1rem;
        padding: 0.75rem 1.25rem;
    }

    /* Buttons */
    .btn-primary {
        background: #000;
        border: 2px solid #f5c518;
        border-radius: 12px;
        padding: 12px 28px;
        font-weight: 600;
        color: #f5c518;
        transition: all 0.3s ease-in-out;
    }

        .btn-primary:hover {
            background: #f5c518;
            color: #000;
            transform: translateY(-2px) scale(1.05);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
        }

    .btn-outline-secondary {
        border: 2px solid #444;
        border-radius: 12px;
        padding: 12px 28px;
        font-weight: 600;
        color: #444;
        transition: all 0.3s;
    }

        .btn-outline-secondary:hover {
            background-color: #444;
            color: #fff;
            transform: translateY(-2px) scale(1.03);
        }

    /* Cards */
    .card {
        border-radius: 18px;
        border: 1px solid #f5c51820;
        transition: transform 0.3s, box-shadow 0.3s;
        background: #fff;
        height: 100%;
        overflow: hidden;
    }

        .card:hover {
            transform: translateY(-8px);
            box-shadow: 0 15px 35px rgba(0,0,0,0.25);
            border-color: #f5c518;
        }

        .card img {
            transition: transform 0.3s ease;
            object-fit: cover;
            width: 100%;
            max-height: 220px;
            border-top-left-radius: 18px;
            border-top-right-radius: 18px;
        }

        .card:hover img {
            transform: scale(1.05);
        }

    .card-title {
        font-size: 1.25rem;
        font-weight: 700;
        color: #111;
    }

    .card-text {
        font-size: 1rem;
        color: #555;
        line-height: 1.5;
    }

    .card-footer {
        background: transparent;
        border-top: none;
    }

    .price-tag {
        font-size: 1.3rem;
        color: #f5c518;
        font-weight: 700;
    }
</style>

<!-- Hero Section -->
<!-- Hero Section -->
<section class="hero text-white text-center d-flex align-items-center">
    <div class="container hero-content">
        <h1 class="display-4 fw-bold">Only 3 Steps Rent</h1>
        <p class="lead">Drive Your Dreams</p>

        <form asp-controller="Home" asp-action="SearchAvailable" method="get" class="row g-3 justify-content-center mt-4">
            <div class="col-md-3">
                <label for="pickupDate" class="form-label">Pickup Date</label>
                <input type="date" id="pickupDate" name="pickupDate" class="form-control form-control-lg" required />
            </div>
            <div class="col-md-3">
                <label for="dropoffDate" class="form-label">Drop-off Date</label>
                <input type="date" id="dropoffDate" name="dropoffDate" class="form-control form-control-lg" required />
            </div>
            <div class="col-md-2 d-flex align-items-end">
                <button type="submit" class="btn btn-warning w-100 btn-lg">Search</button>
            </div>
        </form>
    </div>
</section>



<script>
    const hero = document.querySelector('.hero');
    const images = [
        'https://images.pexels.com/photos/170811/pexels-photo-170811.jpeg',
        'https://images.pexels.com/photos/112460/pexels-photo-112460.jpeg',
        'https://images.pexels.com/photos/244206/pexels-photo-244206.jpeg',
        'https://images.pexels.com/photos/119435/pexels-photo-119435.jpeg',
        'https://images.pexels.com/photos/3311574/pexels-photo-3311574.jpeg'

    ];
    let current = 0;

    function changeHeroImage() {
        current = (current + 1) % images.length;
        hero.style.backgroundImage = `url('${images[current]}')`;
    }

    // Initial image
    hero.style.backgroundImage = `url('${images[0]}')`;

    // Change every 5 seconds
    setInterval(changeHeroImage, 5000);
</script>

    <h2 class="mb-4">Available Cars</h2>
    <div class="row g-4">
        @foreach (var car in Model)
        {
            <div class="col-sm-6 col-md-4">
                <div class="card shadow-lg" style="width: 18rem; margin:auto;">

                    <!-- Car Image -->
                    <img src="@car.ImageUrl"
                         class="card-img-top"
                         alt="@car.CarName"
                         style="height:200px; object-fit:cover;"
                         onerror="this.src='/images/cars/default.png'" />

                    <!-- Card Body -->
                    <div class="card-body">
                        <h5 class="card-title">@car.CarName (@car.CarModel)</h5>

                        <p class="card-text mb-2">
                            
                            <strong>Seats:</strong> @car.SeatingCapacity <br />
                      
                        </p>

                        <p class="fw-bold text-warning mb-2">
                            Price/Day: @car.PricePerDay.ToString("C")
                        </p>

                        @if (car.IsAvailable)
                        {
                            <span class="badge bg-success mb-3">Available</span>
                        }
                        else
                        {
                            <span class="badge bg-danger mb-3">Booked</span>
                        }
                        <div class="d-flex justify-content-between">
                        <a asp-controller="CustomerProfile"
                           asp-action="Details"
                           asp-route-id="@car.CarID"
                           class="btn btn-primary">
                            View
                        </a>
                        <a asp-controller="Bookings"
                           asp-action="Create"
                           asp-route-carId="@car.CarID"
                           asp-route-pickupDate="@DateTime.Today.ToString("yyyy-MM-dd")"
                           asp-route-returnDate="@DateTime.Today.AddDays(1).ToString("yyyy-MM-dd")"
                           class="btn btn-primary">
                            Book Now
                        </a>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>

<footer class="footer py-4">
    <div class="container">
        <div class="row align-items-center">
            <!-- Company Info -->
            <div class="col-md-4 text-center text-md-start">
                <h4 class="fw-bold mb-1">MotoFlex</h4>
                <p class="mb-0">Your trusted partner for premium car rentals</p>
                <p class="mb-0">📧 contact@motoflex.com | 📞 +94 77 123 4567</p>
            </div>

            <!-- Navigation Links -->
            <div class="col-md-4 text-center my-2 my-md-0">
                <a asp-controller="Home" asp-action="Index" class="footer-link">Home</a> |
                <a asp-controller="Home" asp-action="Privacy" class="footer-link">Privacy Policy</a> |
                <a asp-controller="Home" asp-action="Terms" class="footer-link">Terms & Conditions</a>
            </div>

            <!-- Social Icons & Payment -->
            <div class="col-md-4 text-center text-md-end">
                <!-- Social -->
                <a href="https://www.facebook.com" target="_blank" class="social-link me-2">
                    <i class="bi bi-facebook"></i>
                </a>
                <a href="https://www.youtube.com" target="_blank" class="social-link me-2">
                    <i class="bi bi-youtube"></i>
                </a>
                <a href="https://www.instagram.com" target="_blank" class="social-link me-2">
                    <i class="bi bi-instagram"></i>
                </a>

                <!-- Payment -->
                <img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcRdFzkATbMbrcUZleHDLQAsFQecA6L7kbpbTg&s" alt="Visa" class="mx-2" />
            </div>
        </div>

        <!-- Bottom Row: Copyright -->
        <div class="row mt-3">
            <div class="col text-center">
                <p class="mb-0" style="font-size:0.9rem; color:#aaa;">
                    © @DateTime.Now.Year MotoFlex. All rights reserved.
                </p>
            </div>
        </div>
    </div>
</footer>




<style>
    .footer {
        background: #111;
        color: #f5c518;
        border-top: 3px solid #f5c518;
        margin-top: 0 !important; /* Remove gap above footer */
    }

    .footer-link {
        color: #f5c518;
        text-decoration: none;
        margin: 0 5px;
        font-weight: 500;
        transition: color 0.3s;
    }

        .footer-link:hover {
            color: #fff;
        }

    .social-link {
        color: #f5c518;
        font-size: 1.3rem;
        transition: color 0.3s;
    }

        .social-link:hover {
            color: #fff;
        }

    .footer img {
        height: 30px;
        filter: grayscale(20%);
        transition: transform 0.3s;
    }

        .footer img:hover {
            transform: scale(1.1);
        }

</style>

