@model CarRentalManagement.Models.User

@{
    ViewData["Title"] = "User Details";
}

<h2>User Details</h2>

<dl class="row">
    <dt class="col-sm-2">User ID</dt>
    <dd class="col-sm-10">@Model.UserID</dd>

    <dt class="col-sm-2">First Name</dt>
    <dd class="col-sm-10">@Model.FirstName</dd>

    <dt class="col-sm-2">Last Name</dt>
    <dd class="col-sm-10">@Model.LastName</dd>

    <dt class="col-sm-2">Nationality</dt>
    <dd class="col-sm-10">@Model.Nationality</dd>

    <dt class="col-sm-2">Email</dt>
    <dd class="col-sm-10">@Model.Email</dd>

    <dt class="col-sm-2">Phone</dt>
    <dd class="col-sm-10">@Model.Phone</dd>

  
    <dt class="col-sm-2">ID Number</dt>
    <dd class="col-sm-10">@Model. NationalID</dd>

    <dt class="col-sm-2">Passport</dt>
    <dd class="col-sm-10">@Model. PassportNumber</dd>

    <dt class="col-sm-2">Username</dt>
    <dd class="col-sm-10">@Model.Username</dd>

    <dt class="col-sm-2">Password</dt>
    <dd class="col-sm-10">******</dd>

    <dt class="col-sm-2">Role</dt>
    <dd class="col-sm-10">@Model.Role</dd>
</dl>

<a asp-action="Index" class="btn btn-secondary">⬅ Back to List</a>
